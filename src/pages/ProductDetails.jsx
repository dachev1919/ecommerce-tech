import React from "react";
import Meta from "../components/Meta";

const ProductDetails = () => {
    return (
        <>
            <Meta canonical={`product/id`} title='title' />
        </>
    );
};

export default ProductDetails;